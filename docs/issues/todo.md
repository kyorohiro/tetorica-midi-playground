# MIDI Playground TODO

FM2612 Playgroundとの機能差を整理する。MIDI版は基本的な演奏機能を移した段階であり、同名APIも完全互換ではない。

## 現在の対応範囲

- `play` / `beat` / `setBpm`、MIDIチャンネル・velocity指定。
- `choose` / `scale` / `cycle`。
- `liveLoop`、画面のRun / Stop。Runごとに全ループを再起動する。
- Run file選択（既定 `/index.js`）、ファイル保存・import/export。
- MIDI出力選択時の自動接続、接続先・状態の表示。
- 外部MIDI Clockの監視とC4テスト。JavaScriptの演奏は内部BPMで動作する。

## 優先1: 拍・ループの互換性

- [x] `nextBeat()`：Run開始基準の内部拍境界まで待機。BPM変更で拍位相を保ち、待機中にも反映。境界上では次の拍へ進む。仮想時計によるテストを追加。Workerタイマーによる遅延はあり、サンプル精度ではない。FM版のループcursorBeatとの完全互換は後続工程。
- [ ] `stopLoop(name)` / `stopAllLoops()`：個別・全ループの停止を追加する。待機中の処理、送信済みノートのNote Off、再起動時の扱いを検証する。
- [ ] `cycle()`：FM版のループ単位・呼び出し位置単位の状態管理に合わせる。現在はRun内でカウンターを共有するため、複数ループが干渉し得る。
- [ ] `liveLoop`の更新：FM版の再評価・ループ差し替え動作を調査し、継続する状態とリセットする状態を定義する。現在はRunで全停止・再起動。
- [ ] 並行ループ、awaitをまたぐ状態、停止・再実行をテストする。タイミングの許容誤差も明記する。

## 優先2: 音楽用ヘルパー

- [ ] `chord()`。
- [ ] `rand` / `rrange` / `randInt` / `lerp` / `noteLerp`。
- [ ] FM版の引数、戻り値、エラー条件を確認して移植する。MIDIの音域制限など必要な差分は明記する。
- [ ] 既存の`play` / `beat` / `scale`等も、FM版との意味・待機動作の差を確認する。
- [ ] 各ヘルパーの境界値と、実際のWorkerからMIDI送信までのテストを追加する。

## 優先3: 編集・実行環境

- [ ] キーボードイベント用API。エディター入力との競合、登録解除、Run / Stop時の寿命を決める。
- [ ] ファイル間のmodule import。仮想ファイルの解決とWorker内の実行方法を整える。
- [ ] Monacoの補完・エディター機能。現在はtextarea。
- [ ] Helper画面・英語/日本語README・サンプルを実装済みAPIと一致させる。

## 別工程: 外部MIDI Clockとの同期

- [ ] Clock監視・テストから、スクリプト演奏の同期へ拡張する。
- [ ] Start / Continue / Stop、Clock途絶、BPM変更、再接続時の動作を定義する。
- [ ] `nextBeat()`による内部拍同期と、外部Clock同期を区別して説明・検証する。

## MIDI版への導入方針を別途検討するもの

FM音色、PSG、DAC、サンプル、エフェクトは現在未搭載。
これらはFM版の内蔵音源に関わるため、そのまま全移植するTODOにはしない。
必要に応じて、外部音源へのMIDI制御として実現する機能と、内蔵音声処理を必要とする機能を分けて検討する。

## 完了の目安

- 対応APIと互換性の差がドキュメントから分かる。
- 複数ループでも状態が混ざらず、Stop後に不要なノートが送信されない。
- 自動テストに加え、GarageBandでRun / Stop・再実行を確認する。

## 進捗: ループ専用ヘルパーによる停止・状態分離

- [x] `liveLoop(name, async ({play, beat, nextBeat, cycle}) => ...)` の専用ヘルパーを追加。
- [x] `stopLoop(name)` / `stopAllLoops()`を公開。専用ヘルパーはawait後に停止を確認し、後続ノートを送信しない。
- [x] 専用`cycle`はループ単位・反復内の呼び出し順単位で分離。名前付きキーもループ内で独立。
- [x] 同名置換で旧専用ヘルパーを停止し、新しいカウンターで開始。
- [x] Workerテストで並行ループ、await、同名置換、個別/全停止を検証。

制約: グローバルヘルパーを使う従来コードの暗黙コンテキスト互換は未実装。
その場合は反復境界で停止し、cycleカウンターは共有のまま。
送信済みノートは既存のnativeタイマーで指定時間（最大10秒）後にNote Off。
個別停止時の即時Note Offは、同一チャンネル・同一音を別ループが使う場合の所有権を含めて後続で設計する。
画面Stopは従来どおり全ノート解放。Runをまたぐホット差し替えも未実装。
上の完全互換を目的としたチェック項目は未完了として残す。GarageBand実機確認は未実施。
